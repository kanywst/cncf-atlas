# Getting Started

> Verified against `{version}`. Commands assume {prerequisites}.

## Prerequisites

- {requirement}

## Install

```bash
{install command}
```

## A first working setup

{The shortest path to something that actually runs and does the tool's core job. Number the steps; show real commands and the expected output.}

1. {step}

```bash
{command}
```

## Verify it works

{How to confirm the setup is healthy: a status command, an endpoint, a log line to look for.}

## Where to go next

{Pointers to the official docs for production concerns such as HA, security hardening, and scaling, without re-documenting them here.}
