# 内部実装

> コミット `{short-sha}` のソースを読んだもの。ここでの主張はすべてファイルと行を指すこと。

## コードマップ

{重要なディレクトリと、各々に何があるか。生成コード・vendor は除く。}

| パス | 責務 |
| --- | --- |
| `{path}` | {何をするか} |

## 中核データ構造

{システム全体が回転する数少ない型: reconciler の状態、ポリシー AST、リクエストコンテキストなど。どこで定義され、どんな不変条件を保つか。}

## 追う価値のあるパス

{重要な操作を 1 つ選び、実際のコードを歩く: エントリポイント、主要な分岐、本処理の場所、永続化・応答の場所。`file:line` アンカーと短い引用で。}

```text
{短い説明用スニペットまたは呼び出しチェーン}
```

## 読んで驚いた点

{コードを読まないと分からない非自明な設計: 整合性の話を変えるキャッシュ、ファストパス、微妙なロック。プレスリリースには絶対に載らない部分。}
